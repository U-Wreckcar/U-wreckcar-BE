const {
    asyncWrapper, //
} = require('../utils/middleware');

module.exports = {
    // 생성관련
    create: {},

    // 변경 관련
    update: {},

    // 조회 관련
    get: {},

    // 삭제 관련
    delelte: {},
};
