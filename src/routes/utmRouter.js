import express from 'express';
const router = express.Router();

// UTM 관련
router.get('/api/utm', () => {});
router.get('/api/utm', () => {});
router.delete('/api/utm', () => {});
router.post('/api/utm', () => {});
router.post('/api/utm', () => {});
router.post('/api/utm', () => {});
router.post('/api/utm', () => {});
router.post('/api/utm', () => {});

export { router };
